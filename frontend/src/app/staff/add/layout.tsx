import React from 'react'
import AdminPage from './page'  // Import AdminPage if needed

const Layout = () => {
    return (
        <div>
            <AdminPage />  {/* AdminPage is the page where AddForm is rendered */}
        </div>
    )
}

export default Layout
